using UIKit;

namespace MauiSampleApp.Platforms.iOS
{
    public class Program
    {
        private static void Main(string[] args)
        {
            UIApplication.Main(args, null, typeof(AppDelegate));
        }
    }
}